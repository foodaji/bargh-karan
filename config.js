/* ============================================================
   تنظیمات و کلیدهای سامانه «برق‌فاکتور»
   ============================================================
   این فایل، سامانه را به یک Bin در JSONBin.io وصل می‌کند که نقش
   «پایگاه‌داده‌ی ابری مشترک» را دارد (کاربران، نرخنامه، فاکتورها،
   پیام‌های تماس). این‌ها سه مقداری هستند که باید بعد از انجام مراحل
   راه‌اندازی (فایل README همراه این پروژه) پر کنید:
   ============================================================ */

const JSONBIN_CONFIG = {
  // شناسه‌ی Bin که در جسون‌بین ساختید — چیزی شبیه "64f1a2b3c4d5e6f7a8b9c0d1"
  binId: "6918475dd0ea881f40e9c02b",

  // یک Access Key (نه Master Key!) با دسترسی Read + Update روی این Bin
  apiKey: "$2a$10$cz8BByqir9jDZ4AfVbfIP.KYa26rdZhTQ4D/v4vIQRhRYI.mMr5yK",

  // آدرس پایه‌ی API جسون‌بین — نیازی به تغییر ندارد
  baseUrl: "https://api.jsonbin.io/v3/b",
};

/* ------------------------------------------------------------
   ⚠️ هشدار امنیتی بسیار مهم — حتماً بخوانید
   ------------------------------------------------------------
   این پروژه یک سایت کاملاً استاتیک است (بدون هیچ سروری پشت آن)،
   بنابراین این فایل و مقدار apiKey داخل آن روی GitHub Pages به
   صورت متن ساده و کاملاً قابل مشاهده برای هر کسی است — هرکسی که
   View Page Source یا Network Tab مرورگر را باز کند، این کلید را
   می‌بیند و می‌تواند با آن به Bin شما بخواند/بنویسد.

   برای کاهش (نه حذف کامل) این ریسک:
   • حتماً از یک Access Key محدود (Read + Update) استفاده کنید، نه
     Master Key اصلی حساب‌تان (که دسترسی کامل به همه‌چیز دارد).
   • یک حساب JSONBin جداگانه فقط برای همین پروژه بسازید؛ آن را برای
     هیچ داده‌ی حساس دیگری استفاده نکنید.
   • رمزهای عبور کاربران در این نسخه به‌صورت متن ساده (نه هش‌شده)
     در همان Bin ذخیره می‌شوند — پس این سامانه را برای داده‌های واقعاً
     حساس یا تعداد زیاد کاربر ناشناس مناسب ندانید.
   ------------------------------------------------------------ */
